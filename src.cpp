#include <iostream>
#include <chrono>
#include <thread>

wait(int seconds){
    std::this_thread::sleep_for(std::chrono::milliseconds(s));
}

int main(){
    std::cout << "aight cya";
    wait(1);

    exit(0);
    
    return 0;
}